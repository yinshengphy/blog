name: Deploy Astro Blog to K3s

on:
  push:
    branches:
      - master
    paths:
      - 'src/**'
      - 'public/**'
      - 'astro.config.mjs'
      - 'tsconfig.json'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

concurrency:
  group: blog-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on:
      - self-hosted
      - blog
    timeout-minutes: 20
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          clean: false

      - name: Use domestic npm registry
        run: |
          npm config set registry https://registry.npmmirror.com
          npm config set cache "$PWD/.runner-cache/npm"

      - name: Check dependency cache
        id: deps
        run: |
          set -euo pipefail
          mkdir -p .runner-cache
          cat package.json package-lock.json | sha256sum | awk '{print $1}' > .runner-cache/deps.sha.current

          if [ -d node_modules ] && [ -f .runner-cache/deps.sha ] && cmp -s .runner-cache/deps.sha.current .runner-cache/deps.sha; then
            echo "install=false" >> "$GITHUB_OUTPUT"
            echo "Dependencies unchanged and node_modules exists -> skip install"
          else
            echo "install=true" >> "$GITHUB_OUTPUT"
            echo "Dependencies missing or changed -> run npm ci"
          fi

      - name: Install dependencies
        if: steps.deps.outputs.install == 'true'
        run: |
          npm ci --prefer-offline --no-audit --no-fund --cache "$PWD/.runner-cache/npm"
          cp .runner-cache/deps.sha.current .runner-cache/deps.sha

      - name: Build
        run: npm exec -- astro build

      - name: Publish to K3s PVC
        run: |
          test -d dist
          mkdir -p /deploy/public
          if command -v rsync >/dev/null 2>&1; then
            rsync -a --delete dist/ /deploy/public/
          else
            find /deploy/public -mindepth 1 -maxdepth 1 -exec rm -rf {} +
            cp -a dist/. /deploy/public/
          fi
          echo "Published $(du -sh dist | cut -f1) to PVC"

      - name: Sync markdown source for RAG indexer
        run: |
          mkdir -p /deploy/content/posts
          if command -v rsync >/dev/null 2>&1; then
            rsync -a --delete src/content/posts/ /deploy/content/posts/
          else
            find /deploy/content/posts -mindepth 1 -maxdepth 1 -exec rm -rf {} +
            cp -a src/content/posts/. /deploy/content/posts/
          fi
          echo "Synced markdown posts for RAG indexer"

      - name: Trigger RAG indexer asynchronously
        run: |
          if command -v kubectl >/dev/null 2>&1; then
            kubectl -n blog create job --from=cronjob/blog-indexer "blog-indexer-manual-${GITHUB_RUN_ID}" || true
          else
            echo "kubectl not found, skip manual index trigger; CronJob will catch up"
          fi
